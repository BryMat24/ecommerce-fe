/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: false,
    images: {
        domains: ['images.samsung.com', 'images.unsplash.com'], // Add any other domains your images are hosted on
    },
};

export default nextConfig;
